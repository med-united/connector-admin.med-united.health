<mvc:View
    controllerName="sap.f.ShellBarWithFlexibleColumnLayout.controller.EnabledAvailable"
    xmlns="sap.m"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns:f="sap.f"
    xmlns:card="sap.f.cards"
    displayBlock="true"
    height="100%"
>
    <ScrollContainer
        height="100%"
        width="100%"
        vertical="true"
    >
        <VBox renderType="Bare">
            <Label text=" " />
            <Label text="Enabled" />
            <f:GridContainer
                id="list1"
                class="sapUiSmallMargin"
                width="100%"
                snapToRow="true"
                items="{ path: '/' }"
            >
                <f:layoutXS>
                    <f:GridContainerSettings
                        rowSize="70px"
                        columnSize="70px"
                        gap="8px"
                    />
                </f:layoutXS>

                <f:Card>
                    <f:layoutData>
                        <f:GridContainerItemLayoutData
                            minRows="{rows}"
                            columns="{columns}"
                        />
                    </f:layoutData>
                    <f:header>
                        <card:Header title="{title}" />
                    </f:header>
                </f:Card>
            </f:GridContainer>
            <Label text="Disabled" />
            <f:GridContainer
                id="grid1"
                class="sapUiSmallMargin"
                width="100%"
                snapToRow="true"
                items="{ path: '/' }"
            >
                <f:layoutXS>
                    <f:GridContainerSettings
                        rowSize="70px"
                        columnSize="70px"
                        gap="8px"
                    />
                </f:layoutXS>

                <f:Card>
                    <f:layoutData>
                        <f:GridContainerItemLayoutData
                            minRows="{rows}"
                            columns="{columns}"
                        />
                    </f:layoutData>
                    <f:header>
                        <card:Header title="{title}" />
                    </f:header>
                </f:Card>
            </f:GridContainer>
        </VBox>
    </ScrollContainer>
</mvc:View>
